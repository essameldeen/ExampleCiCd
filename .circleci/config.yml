  version: 2.1
  jobs:
    try-job:
      macos:
        xcode:14.0.1
      steps:
        - run: echo "I am try a job named try job within a work flow named try-workflow"


  workflows:
    try-workflow:
      jobs:
        - try-job