version: 2.1

jobs:
  beta-android:
    docker:
      - image: circleci/android:api-30-node
    steps:
      - checkout
      - run: yarn beta

workflows:
  beta-android:
    jobs:
      - beta-android